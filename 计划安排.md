# 9天完成要求品类下5000张原产品图的背景替换处理

# 计划安排

## part1.
完成下列品类中5000张原产品图的筛选
90 - Coffee Tables
114 - Benches
88 - TV Stands & Entertainment Centers
91 - End & Side Tables
83 - Sofa
176 - Room Dividers
92 - Cabinets & Chests
82 - Accent Chairs
218 - Plant Stands & Tables
105 - Hall Trees & Coat Racks
104 - Console Tables
85 - Bookcases

### 方法：
1. 获取该品类下所有spu以及图片向量（7万个，每个图片向量768维度，先前存储在elasticsearch中）
    - 筛选去掉无效状态
    - 筛选未存储图片向量的spu

2. 清洗相似度过高的spu
    - 按品类分别清洗
    - 计算品类下所有图片两两之间的相似度，找出相似对（相似度大于0.95） similar_pairs: [(spu1, spu2, sim), ...]
    - 遍历相似对，统计每个每个spu被标记的次数 similarity_counts = {spu1: count1, spu2: count2, ...}
    - 对于每个相似对 (spuA, spuB, sim)，选择保留被标记次数少的那个 spux
      - 如果次数相同，保留spu id较小的那个

## part2:
完成所有B图库的构建

结果：
8万个doc
{
    "image_id": "xxxx",
    "spu_id": "xxxx",
    "category": "xxxx",
    "image_vector": [...],
    "cluster_id": "xxx",
    "used_num": xx,
}

cluster mapping
{
    "90 - Coffee Tables": [
        "cluster_1": [...],  # 质心向量
        "cluster_2": [...],
        ...
    ]
    ...
}

### 方法：
1. 对每个品类下的所有图片向量进行聚类
   - 方法：K-Means
   - 聚类数目：N
        - N = min(50, max(10, int(sqrt(M/100))))
        - M: 该品类下的图片总数
   - 输出：每个品类下的聚类质心向量以及其对应的簇ID

2. 计算每个图片向量所属的簇ID
   - 方法：计算图片向量与各个质心向量的距离，选择距离最近的质心对应的簇ID

3. 构建B图库
    - 对每个图片，存储其图片ID、spu ID、品类ID、图片向量、簇ID、使用次数（初始为0）
    - 存储到Elasticsearch中，便于后续检索




## part3.
完成采样算法的构建
交付结果：能够根据A图以及其品类，采样出n张B图









part3.
comfyui api部署
标准：能通过API接口调用生成图像

part4.
生图应用程序的构建
交付：完成5000张原产品图的背景替换
