# 场景图批量替换功能文档

**目的**：批量更换产品A的背景，放到站群上售卖

- 多样性、质量



**场景图替换原理**：原产品图A + 场景图B + 描述场景图B内的元素、风格、氛围、场景的prompt ---> 新的场景图C



快速实现功能： 

1. 场景图B的采样
2. 场景 prompt 的生成
3. Autodl 模型部署与应用





### 场景图B的采样算法

**单个 A 图 → 生成 n 个 C 图**的情况：

- A 与每个 B 要尽可能不同（保证 A-B 差异性）；
- 同一 A 下的 B1, B2, ..., Bn 之间也要尽量不同（保证 B-B 内部多样性）



**输入**：

- A 图向量 `a_vec`
- B 图候选集合 `{b1, b2, ..., bm}` 的向量 `b_vecs`
- 需要选出 n 个 B

**优化目标**：

- 选出的 B 图要同时满足：
  1. A-B 相似度低（与 A 尽量不同）
  2. 选中的 B-B 两两之间相似度低（内部多样性）





#### **预处理：**

1. Elastic search 中每个图片向量文档需要记录该商品的品类
2. 每一个品类下，图片需要进行聚类处理
3. 记录每个簇的质心



#### 聚类：分层式动态聚类

不同品类下：

**产品图小于1000**：则不进行聚类操作

**产品图大于1000， 小于10000**：

初次聚类：通过kmeans聚类，并记录每个聚类的中心向量

后续新增图片都直接knn就算与中心向量的距离，直接分配类别，并更新质心



**产品图大于10000**：

以10000为单位分层

初次聚类：使用kmeans

层内增量聚类：knn

数量增长到跨层：重新使用kmeans聚类



流程：

##### 初始聚类

- 新品类首次出现 → 跑一次 KMeans 得到 K 个簇 + 质心存储

##### 新增图片分配（N 未跨层时）

- 新增图片向量 v
- 计算 v 与所有簇质心 ck 的 KNN 距离
- 分配给最近簇，**在线更新质心**（加权平均）：

##### 跨层触发重聚类

- 当累计样本数 N 跨过上面定义的层级阈值时：
  1. 重新计算 K（根据新的 N）
  2. 取全量向量重新跑 KMeans，得到新的簇划分
  3. 更新质心表 & 各图片的 cluster_id



elastic search场景图向量库文档字段要求：

```python
{
  "image_vector":[...],
  "文档ID": xxxx,
  "category": 类目ID
  "cluster_id": xxx	# 簇ID,
  "used_num": xx	# 使用次数
}
```







### **采样流程（单个A）**

假设要个A生成n张新图：

1. 第一层：与A差异最大
   - 在每个簇里，检索找出与A差异大的top-K 候选
2. 第二层：冷门B优先
   - 候选池中每张图一个加权分：score=α⋅(1−similarity(A,B))+β⋅(1−use_count(B))
     - similarity(A,B) A与B的相似度
     - use_count(B): B 图已被使用次数归一化
     - α, β : 控制差异性与冷门优先的权重

3. 第三层： B之间差异最大
   1. 从排序好的额候选池里使用Max-Min（下一个选的 B **离所有已选 B 图都尽量远**）采样:
      1. 先选分数最高的B1
      2. 后续每次选与已选B集合中最接近的B 距离最大的那个
4. 输出n张B图





### 场景图prompt生成

使用大模型识别B图的风格、场景、元素内容，生成prompt

输入：B图

输出：prompt



### Autodl场景图生成模型应用

接收：产品图A， n组（场景图B，prompt对）

输出：n个结果图C













后续优化：

1. 新维护一套场景图向量库
   1. 使用clip模型
   2. 标签维度设计

| 维度         | 示例标签                     | 作用             |
| ------------ | ---------------------------- | ---------------- |
| SceneType    | 餐厅、厨房、客厅             | 确保同类场景     |
| Style/Theme  | 北欧、工业风、日式、现代     | 控制室内风格     |
| Key Elements | 落地窗、吊灯、地毯、植物\... | 让背景差异具体化 |
| Color Scheme | 暖色、冷色、中性色           | 光影与色调变化   |
|              |                              |                  |



